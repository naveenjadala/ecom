export const navigate = jest.fn();
export const setParams = jest.fn();

export const navigationMock = {
  navigate,
  // Add other navigation methods as needed
};

export const bottomTabNavigationMock = {
  navigate,
  setParams,
  // Add other navigation methods as needed
} as any;
