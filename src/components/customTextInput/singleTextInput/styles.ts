import {StyleSheet} from 'react-native';

export const customInputStyles = StyleSheet.create({
  container: {
    flex: 1,
    width: '100%',
  },
  text: {
    borderColor: 'black',
    borderWidth: 0.5,
    borderRadius: 5,
    padding: 15,
  },
});
